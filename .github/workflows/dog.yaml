name: reviewdog
on:
  pull_request:
    branches:
      - main
jobs:
  # 構文チェック
  lint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
        with:
          fetch-depth: 0
      # 1. reviewdog の setup action を追加
      - uses: reviewdog/action-setup@v1
        with:
          reviewdog_version: latest
      - name: code snifffer install
        run: composer require squizlabs/php_codesniffer
      - name: lint
        env:
          # 2. reviewdog が コメントを書き込めるように token をセットする
          REVIEWDOG_GITHUB_API_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        # 3. 構文チェックの結果を reviewdog へ渡す
        run: git diff --name-only origin/${{ github.base_ref }}..${{ github.head_ref }} | xargs ./vendor/bin/phpcs --report=emacs --standard=phpcs.xml | reviewdog -name="phpcs" -diff="git diff origin/${{ github.base_ref }}..${{ github.head_ref }}" -reporter=github-pr-review -efm='%f:%l:%c:%m'
  # 静的解析
  analyse:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
        with:
          fetch-depth: 0
      - name: tmp
        run: git diff --name-only origin/${{ github.base_ref }} ${{ github.head_ref }}
      # 1. reviewdog の setup action を追加
      - uses: reviewdog/action-setup@v1
        with:
          reviewdog_version: latest
      - name: phpstan install
        run: composer require phpstan/phpstan
      - name: analyse
        env:
          # 2. reviewdog が コメントを書き込めるように token をセットする
          REVIEWDOG_GITHUB_API_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        # 3. 静的解析の結果を reviewdog へ渡す
        run: git diff --name-only origin/${{ github.base_ref }} HEAD ${{ github.head_ref }} | xargs -IXXX ./vendor/bin/phpstan analyse XXX --error-format=raw -l 5 | reviewdog -name="phpstan" -f=phpstan -diff="git diff origin/${{ github.base_ref }} HEAD ${{ github.head_ref }}" -reporter=github-pr-review
